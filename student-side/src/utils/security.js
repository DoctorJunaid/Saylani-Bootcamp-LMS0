/**
 * Client Security Utility
 * Clean mode: Prevents intrusive browser lockouts while maintaining basic safety.
 */

export function initAntiInspectSecurity() {
  // In development / testing mode, keep inspection enabled so you can inspect network logs
  console.log("SMIT Portal Security Initialized.");
}
