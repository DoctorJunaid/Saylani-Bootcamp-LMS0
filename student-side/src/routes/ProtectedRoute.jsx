import React from "react";

/**
 * ProtectedRoute Component
 * (Auth restriction disabled for direct UI development & preview)
 */
export const ProtectedRoute = ({ children }) => {
  return children;
};
